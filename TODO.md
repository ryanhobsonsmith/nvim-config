# LazyVim Migration TODO

## Configure

- [ ] Configure markdown linting rules (`.markdownlint-cli2.yaml`) — disable noisy rules like line-length (MD013), bare URLs (MD034)
- [ ] Try configuring tsgo as lsp for better performance

## To research

- Harpoon and various file navigation techniques (marks, jump lists, issue lists, etc...)
- LSP Navigation (jump to def/usage/implementation, peek definition, etc...) Search for functions/vars/etc...
- Lazygit keybindings to see if we can make them more intuitive for nvim/tmux/etc... espeically for scrolling
- In file code navigation/jumping.  
- terminal use inside of neovim
- debugging inside of neovim
- running tests inside of neovim

## Learn & Practice

- [ ] File explorer and file navigation (neo-tree, fuzzy finding, buffer switching)
- [ ] Copy/paste techniques and clipboard integration (shell, tmux, OS clipboard)
- [ ] TypeScript code editing workflow (go-to-definition, type inspection, linting, testing, debugging)
- [ ] Git merge/conflict resolution and viewing diffs in the terminal (replacing VS Code workflow)
- [ ] Advanced nvim movement and editing (beyond basics — text objects, motions, macros, registers, etc.)
- [ ] Quickly moving blocks of code (commenting, indenting, moving lines up/down, duplicating)
- [ ] Multi-cursor editing or nvim equivalents (visual block mode, macros, `:s`, etc. to replace VS Code multi-cursor habits)
